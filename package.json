{
  "scripts": {
    "prebuild": "grep -R \"export const config\" -n app || true",
    "build": "next build",
    "start": "next start"
  }
}

{
  "name": "blog-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.42.0",
    "next": "14.2.4",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "engines": { "node": ">=18.17.0" }
}
